---
title: 'Get Price Quote'
api: 'POST /request/price'
description: 'Calculate estimated price for an event based on dates and rate'
---

Calculates the estimated value of a reservation based on the duration (hours between start and end date) and the selected rate. Internally calls the SQL function `calculate_value` which determines the price according to time slots, base values, and extra hours.

## Request Body

<ParamField body="v_init_date" type="string" required>
  Event start date and time (ISO 8601 format)
  
  Example: `"2025-01-10T10:00:00.000Z"`
</ParamField>

<ParamField body="v_end_date" type="string" required>
  Event end date and time (ISO 8601 format). Must be after v_init_date.
  
  Example: `"2025-01-10T18:00:00.000Z"`
</ParamField>

<ParamField body="v_fk_rate" type="integer" required>
  Rate ID used to calculate the value (minimum 1)
  
  Example: `2`
</ParamField>

## Response

<ResponseField name="result" type="number">
  Calculated value based on duration and rate
  
  Example: `450000`
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.example.com/request/price \
  -H "Content-Type: application/json" \
  -d '{
    "v_init_date": "2025-01-10T10:00:00.000Z",
    "v_end_date": "2025-01-10T18:00:00.000Z",
    "v_fk_rate": 2
  }'
```

```javascript JavaScript
const response = await fetch('https://api.example.com/request/price', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    v_init_date: '2025-01-10T10:00:00.000Z',
    v_end_date: '2025-01-10T18:00:00.000Z',
    v_fk_rate: 2
  })
});

const data = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.example.com/request/price',
    json={
        'v_init_date': '2025-01-10T10:00:00.000Z',
        'v_end_date': '2025-01-10T18:00:00.000Z',
        'v_fk_rate': 2
    }
)

data = response.json()
```

</RequestExample>

<ResponseExample>

```json 200 Success
{
  "result": 450000
}
```

```json 400 Validation Error
{
  "message": "La fecha final debe ser mayor que la fecha de inicio"
}
```

```json 400 Date Inconsistency
{
  "message": "INCONGRUENCIA EN LAS FECHAS"
}
```

```json 500 Server Error
{
  "message": "Error interno del servidor"
}
```

</ResponseExample>

## Notes

- The endpoint does not require authentication
- The end date must be after the start date, otherwise validation will fail
- The price calculation is based on the rate configuration and duration in hours
- Useful for providing clients with instant price quotes before submitting a request